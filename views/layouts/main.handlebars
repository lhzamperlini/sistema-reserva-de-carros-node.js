<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viagens Defensoria</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav>
        {{#if session.userid}}
        <a href="/trips/dashboard" id="logo">
            <img src="/img/logoDefensoria.png" alt="">
        </a>
        {{else}}
        <a href="/trips/dashboard" id="logo">
            <img src="/img/logoDefensoria.png" alt="">
        </a>
        {{/if}}
        <ul>
            {{#if session.userid}}
            <li><a href="/">Nova Viagem</a></li>
            <li><a href="/trips/pending">Viagens Pendentes</a></li>
            <li><a href="/trips/approved">Viagens Aprovadas</a></li>
            <li><a href="/logout">SAIR</a></li>
            {{else}}
            <li><a href="/login">ENTRAR</a></li>
            <li><a href="/register">CADASTRAR</a></li>
            {{/if}}
        </ul>
    </nav>

    <div class="container">
        {{#if messages.message}}
        <div class="message">
            {{messages.message}}
        </div>
        {{/if}}
        {{#if message}}
        <div class="message">
            {{message}}
        </div>
        {{/if}}
        {{{body}}}
    </div>
    <footer>
        <p><span>Defensoria Pública do Estado do Paraná</span> &copy; 2022</p>
    </footer>
</body>

</html>