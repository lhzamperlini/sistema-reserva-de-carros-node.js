<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viagens Defensoria</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav>
        <a href="/" id="logo">
            <img src="/img/logoDefensoria.png" alt="">
        </a>
        <ul>
            {{#if session.userid}}
            <li><a href="/logout">SAIR</a></li>
            {{else}}
            <li><a href="/login">ENTRAR</a></li>
            {{/if}}
        </ul>
    </nav>

    <div class="container">
        {{#if messages.message}}
        <div class="message">
            {{messages.message}}
        </div>
        {{/if}}
        {{#if message}}
        <div class="message">
            {{message}}
        </div>
        {{/if}}
        {{{body}}}
    </div>
    <footer>
        <p><span>Defensoria Pública do Estado do Paraná</span> &copy; 2022</p>
    </footer>
</body>

</html>